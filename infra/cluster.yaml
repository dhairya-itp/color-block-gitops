# eksctl config for the talk. Create it the day before, delete it right after.
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: ctp-demo
  region: ap-south-1
managedNodeGroups:
  - name: demo
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
